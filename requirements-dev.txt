-r requirements.txt
pytest>=8.0,<10.0
pytest-asyncio>=0.23,<2.0
pytest-cov>=5.0,<8.0
ruff>=0.8,<1.0
# ST-07: gradual 타입 검사. 배포 게이트가 아님 — 정보성 도구.
# pyproject.toml [tool.pyright] 가 basic 모드로 core/·repositories/ 만 검사.
pyright>=1.1,<2.0
