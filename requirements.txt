fastapi>=0.129.0,<1.0
uvicorn[standard]>=0.41.0,<1.0
aiosqlite>=0.22.1,<1.0
httpx>=0.28.1,<1.0
beautifulsoup4>=4.13.0,<5.0
yfinance>=1.1.0,<2.0
google-auth>=2.48.0,<3.0
# Explicit — main.py does `from dotenv import load_dotenv`; not a
# transitive dep of anything else here so pip --upgrade would miss it.
python-dotenv>=1.0.0,<2.0
# websockets is pulled in by uvicorn[standard], but kis_ws_manager.py
# imports it directly, so pin it explicitly against accidental removal.
websockets>=12.0,<16.0
# PDF text extraction for the wiki / research-report ingestion pipeline.
# Pure-Python, no system deps, wheel on ARM + Linux. OCR is out of scope.
pypdf>=5.0,<6.0
