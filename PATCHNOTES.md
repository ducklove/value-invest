# 패치노트

운영 서버(`cantabile.tplinkdns.com:3691`) 배포별 변경 이력입니다. **최신 배포가 맨 위**에 옵니다.

각 항목은 `날짜 · 배포 커밋(이전 배포 대비 커밋 수)` 형식입니다.

---

## 2026-06-09 23:28 KST · `84d42a60` (이전 `1809712a` 대비, 2커밋)

### 🆕 새 기능
- **포트폴리오 전체 신규 공시/리포트 알림 + 종목별 3단계 오버라이드** (`84d42a60`)
  - 포트폴리오 조건 알림에 '신규 공시(전체)'·'신규 리포트(전체)' 추가(보유 전 종목, blanket)
  - 종목 분석의 신규 공시/리포트를 3단계(설정안함/꺼짐/켜짐) 드롭다운으로 전환
  - 개별 설정이 전체에 우선 — 켜짐=개별 발송+전체 스킵, 꺼짐=억제(전체 켜져도 제외), 설정안함=전체 따름

### 🧪 내부
- **패치노트 파일(`PATCHNOTES.md`) 도입** (`5d9d2403`) — 운영 배포 이력 누적 기록 시작

---

## 2026-06-09 22:42 KST · `1809712a` (이전 `e37978fa` 대비, 6커밋)

### 🆕 새 기능
- **마켓테이프 전면 개편** (`1809712a`)
  - **지수**: 현재 열린 시장 위주로 노출 — 홈(KOSPI/KOSDAQ)·환율·WTI는 상시, 해외 주가지수(미 SPX/NASDAQ/다우·일 닛케이·홍콩 항셍·상해)는 그 시장이 열렸을 때만(닫히면 종가 stale → 숨김). 개장시간 판별용 `market_sessions.py` 신규(KST 순수 계산, 미 동부 DST 포함, OS 무관 동일 동작)
  - **종목**: 포트폴리오/관심종목 기반 → 시장 전체 기준(시총상위 + 급등락). ±29%↑는 상/하한가(breaking), ±5%↑는 급등/급락(alert), 미달은 노이즈로 제외. 시총상위는 변동과 무관하게 유지
  - **뉴스**: 제목 앞 `[종목명]` 머리표 제거(종목 정보는 클릭용 메타로 유지)
  - 테이프는 전체 시장 기준이라 사용자별로 안 달라짐 → 공용 캐시 키로 통합
- **종목 분석 화면에 개별 종목 알림 추가** (`f3eac614`)
  - 분석 화면 🔔: 가격 도달·일간 등락률(±%)·신규 공시·신규 리포트 알림(로그인 시, 보유 무관)
  - 신규 공시·리포트는 구독 시점 이후만 1회 발화(엣지트리거). 저신호 공시(증권발행실적보고서 등) 제외
  - 포트폴리오 조건 알림에서 '종목 지정가' 생성 옵션은 제거(기존 규칙은 표시·관리 유지)

### 🔧 개선
- **투자정보 UI** (`3a26bd0a`): 국내 지수 hero 박스 테두리/배경 제거(섹션 바탕에 직접 표시), 국채 만기 라벨 `1D`·10년물 국가 추가(이탈리아·캐나다·인도·브라질), 경제캘린더에 현재 시각 파란 라인

### 🐛 버그 수정
- **종목분석 주당배당금 그래프** (`9b003115`): 주간/일간 차트의 주당배당금이 키 부재로 항상 비어 있던 문제 → trailing TTM 배당 합계로 표시(분석 스냅샷 TTL 60분이라 자동 반영)
- **경제캘린더 설정 패널 토글** (`1dc46e00`): `display:flex`가 `hidden`을 무력화해 '⚙ 설정' 버튼이 무반응이던 문제 수정

### 🧪 내부
- **스팩 deep-link JS 테스트 갱신** (`c41632af`): 외부 도구 링크에 `?theme=` 파라미터를 붙이는 production 동작에 맞춰 단언값 보정

---

## 2026-06-09 10:34 KST · `e37978fa` (이전 `3ddf9895` 대비, 2커밋)

### 🆕 기능 / 개선
- **조건 알림 — 규칙별 '🚨 중요 알림'** (`e37978fa`)
  - 포트폴리오 조건 알림 규칙에 '중요' 플래그 신설(`portfolio_alerts.important` 컬럼 + 스키마 마이그레이션)
  - 규칙 추가 폼에 '🚨 중요' 체크박스, 목록에 중요 배지·강조 카드·★ 토글
  - 중요로 지정된 규칙이 발화하면 텔레그램·카카오 메시지를 🚨🚨 강조 헤더로 감싸 전송
  - 중요 토글은 검증/엣지 리셋 없이 가볍게 갱신 → 이미 발송된 규칙을 중요로 바꿔도 같은 날 재발송하지 않음

### 🐛 버그 수정
- **마켓 테이프 투명도 버그 + KOSPI200 제외** (`0b31fb1c`)
  - 화면 상단 고정 시황 테이프가 45초 갱신마다 공용 flash 애니메이션을 타면서 배경색이 1.6초간 투명해져 뒤 콘텐츠가 비치던 문제 → 테이프 한정으로 자체 배경 애니메이션을 끄고 `::after` 오버레이로 분리
  - KOSPI200은 인트라데이 현행화가 안 돼 시세가 stale → 테이프 이벤트에서 제외(일일 브리핑용 스냅샷에는 유지)
